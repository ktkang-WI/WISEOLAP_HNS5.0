export const downloadSet = ({idx, contentList, sortedItemIdx, reportType, paramItemsStr, downloadType}) => {
  // if (downloadType !== 'img') {
  //   const countGridCell = countGridCell(sortedItemIdx);
  //   const countData = countData(sortedItemIdx);
  // }

}